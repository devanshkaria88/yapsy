# Development with hot-reload
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Source is volume-mounted, not copied
EXPOSE 3000

CMD ["npm", "run", "start:dev"]
